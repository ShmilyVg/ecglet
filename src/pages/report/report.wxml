<!--
  此文件是由模板文件 ".dtpl/page/$rawModuleName.wxml.dtpl" 生成的，你可以自行修改模板

  注意：
  1. 如果没有语法高亮，且你使用的是 vscode 编辑器的话，建义在 vscode 插件库中安装 minapp-vscode 插件
-->

<!-- <web-view src="{{reportUrl}}" bindmessage=""></web-view> -->
<view class="container">
    <!-- <image class="report" src="{{reportUrl}}" data-src="{{reportUrl}}" style="width:{{width}}rpx;height:{{height}}rpx" bindtap="onReportPreview" bindload="onImageLoad" /> -->
  <!-- <view bindtouchmove="touchMove" bindtouchstart="touchStart">
    <image class="report" src="{{reportUrl}}" style="width:{{touch.scaleWidth}}px;height:{{touch.scaleHeight}}px" bindload="imageLoad" />
  </view> -->
  <!-- <web-view wx:if="{{ios}}" src="{{reportUrl}}" bindmessage=""></web-view> -->
  <web-view src="{{reportUrl}}" bindload="onloadSuccess"></web-view>
</view>




